"""finstore-local: the self-hosted single-user app (CLI + dashboard).

Public surface = the CLI command shape and dashboard URL paths. The Python
modules under this package are NOT public API; consumers of the library
import `finstore.*` only.
"""
